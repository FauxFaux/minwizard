apply plugin: 'java'
apply plugin: 'application'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile 'io.dropwizard:dropwizard-core:0.9.0-rc3'
}

mainClassName = "com.goeswhere.minwizard.App"

task wrapper(type: Wrapper) {
    gradleVersion = '2.5'
}
